
<p id="notice"><%= notice %></p>
<ul>
  <li ><%= link_to 'Home', root_path %></li>
</ul>
<div class="container">
  <div class="row">
    <%= search_form_for @q do |f| %>
      <%= f.label :selecione_a_Receita_desejada %>:<br>
      <%= f.search_field :category_cont, placeholder: 'Doces, Salgados, Sopas, Carnes, Sobremesas...' %>
      <%= submit_tag "Buscar", class: "btn btn-primary" %><br>
    <% end %><br>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <p> Obs: voce pode selecionar das seguintes formas:</p>
      <p> 1) Categorias = LANCHES, SALGADOS, DOCES, SALADAS, PRATOS QUENTES, PRATOS FRIOS, MASSAS </p>
      <p> 2) Receitas = Pudins, Sopas, Carnes, Peixes, Tipos de salgadinhos ou docinhos, Tipos de massas...</p>
    </div>
  </div>
</div>


<table class="table table-hover table-bordered">
  <thead class="lead">
    <tr>
      <th>Chef da Receita</th>
      <th>Categoria</th>
      <th>Titulo</th>
      <th>Ingredientes</th>
      <th>Modo de fazer</th>
      <th>Avatar</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cookbooks.each do |cookbook| %>
      <tr>
        <td><%= cookbook.chef %></td>
        <td><%= cookbook.category %></td>
        <td><%= cookbook.title %></td>
        <td><%= cookbook.ingredients %></td>
        <td><%= cookbook.description %></td>
        <td><%= cookbook.avatar %></td>
        <td><%= link_to 'Show', cookbook %></td>
        <td><%= link_to 'Edit', edit_cookbook_path(cookbook) %></td>
        <td><%= link_to 'Destroy', cookbook, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table><br>

<%= link_to 'New Cookbook', new_cookbook_path %>
